<form method="post" action="{{ path('app_admin_document_periodical_delete', {'id': periodical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ periodical.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
